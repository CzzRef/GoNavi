# 原始需求：PR 记录节奏化并迁入 vibe

Date: 2026-09-03
Source: 用户当前会话请求（非口述转写）

当前的 PR 文档记录需要优化成有节奏的历史记录，可以新增相关的文档目录。

每个 PR 的记录需包含以下内容：

1. 标题：以「序号+日期+方式+概要」的形式进行留痕。
2. 内部详细记录：需进行完整赘述，包括：
   (a) PR 的完整生命周期（如中间的变更、优化、修改）；
   (b) 相关 PR 的 Readme 文档；
   (c) 相关的 skills 和规则记录（含全局 PR 规则同步）。

之前的相关 PR 记录要进行重构优化，相关的索引链接引用也要进行优化。

以及 task card 应该体现在 vibe（按 CodeNote AI 规则初始化）内相关 spec 记录，不应该在 czz-docs。

Follow-up（同日）: `czz-docs` 里剩余内容可以迁进 `vibe`；PR 相关文档专门新开目录，把之前的记录迁移归档，并统筹相互引用同步。
